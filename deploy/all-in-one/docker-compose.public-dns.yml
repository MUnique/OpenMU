version: '3'

services:
  openmu-startup:
    environment:
      # Anuncia el nombre DNS (resuelto por el cliente) en lugar de una IP fija.
      # Asegúrate de que el registro A de este host esté en Cloudflare como DNS only (nube gris).
      RESOLVE_IP: "mu.server-pups.space"

