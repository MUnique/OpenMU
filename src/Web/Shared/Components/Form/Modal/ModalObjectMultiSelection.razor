@typeparam TItem

@using MUnique.OpenMU.Web.Shared.Components.Modal

<EditForm Model="@this" class="modal-multi-selection">
  <div class="modal-multi-selection__content">
    <CascadingValue Value="@this.PersistenceContext">
      <MultiLookupField TObject="TItem" @bind-Value="@this.Items" Label="@(MUnique.OpenMU.DataModel.ModelResourceProvider.GetTypeCaption<TItem>())"/>
    </CascadingValue>
  </div>

  <div class="modal-multi-selection__actions">
    <button type="submit" @onclick="@SubmitAsync" class="btn btn-primary">@Resources.OK</button>
    <button type="button" @onclick="@CancelAsync" class="btn btn-secondary">@Resources.Cancel</button>
  </div>
</EditForm>
